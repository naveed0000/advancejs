console.log(NaN === NaN);


let obj1  = {x : 10};
let obj2  = {x : 10};
let obj3  = {y : 10};

// Same object value --> that mean's extract memory location has object 

console.log(obj1 === obj2);
console.log(obj1 === obj3);
console.log(obj1 === obj1); 


/* BEST WAY TO LEARN == AND === try a lot thing then an ans came up so we denitely no the ans then you should do try reasoning */ 

/* Made resume has look like a engineer  */